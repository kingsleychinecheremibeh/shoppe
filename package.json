{
  "name": "ecommerce-pern",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend/shoppe\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend/shoppe",
    "start": "concurrently -n backend,frontend -c cyan,magenta \"npm run start --prefix backend\" \"npm run start --prefix frontend/shoppe\"",
    "build": "npm run build --prefix frontend/shoppe",
    "lint": "npm run lint --prefix frontend/shoppe",
    "test": "npm run test --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend/shoppe"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
